import React from "react"

// import { useNavigate } from "react-router-dom";

const Room = ({name}) => {

    return (
        <div className='Room'>
            <h2>Room {name}</h2>
        </div>
    );
}

export default Room;
